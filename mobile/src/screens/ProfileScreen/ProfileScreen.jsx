import React from 'react';

import { Layout, Title } from '../../styles/components';

const ProfileScreen = () => (
  <Layout>
    <Title>Profile</Title>
  </Layout>
);

export default ProfileScreen;
