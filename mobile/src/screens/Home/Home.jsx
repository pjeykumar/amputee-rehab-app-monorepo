import React from 'react';
import { Button, Title } from '../../../styles/components';

const Home = ({ navigation }) => {
  return <Title>React Native with 👩‍🎤 Emotion</Title>;
};

export default Home;
